name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore
        run: dotnet restore JellyfinReplayGain.slnx

      - name: Build
        run: dotnet build JellyfinReplayGain.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test JellyfinReplayGain.slnx --configuration Release --no-build --verbosity normal
